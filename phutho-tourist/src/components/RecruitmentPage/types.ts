// src/types/index.ts (hoặc src/types.ts nếu không có thư mục types)
export type Job = {
  id: string;
  image: string;
  title: string;
  type: string;
  location: string;
  status: string;
  description: string;
  field: string;
};
